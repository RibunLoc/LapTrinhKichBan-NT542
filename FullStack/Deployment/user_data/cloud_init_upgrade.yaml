#cloud-config
package_update: true
package_upgrade: true
runcmd:
  - echo "Cloud-init ensures system packages are upgraded before Ansible runs" > /var/log/cloud-init-upgrade.log

# Check logs after boot
# cat /var/log/cloud-init-output.log
# cat /var/log/cloud-init-upgrade.log
