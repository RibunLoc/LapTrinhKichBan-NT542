- name: Vá bảo mật định kỳ
  hosts: droplet_cis
  become: yes

  tasks:
    - name: Update cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Dist upgrade toàn bộ gói
      apt:
        upgrade: dist
        autoremove: yes
